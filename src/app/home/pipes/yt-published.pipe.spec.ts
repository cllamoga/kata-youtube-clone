import { YtPublishedPipe } from './yt-published.pipe';

describe('YtPublishedPipe', () => {
  it('create an instance', () => {
    const pipe = new YtPublishedPipe();
    expect(pipe).toBeTruthy();
  });
});
