import { YtDurationPipe } from './yt-duration.pipe';

describe('YtDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new YtDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
