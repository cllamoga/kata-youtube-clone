import { YtViewCountPipe } from './yt-view-count.pipe';

describe('YtViewCountPipe', () => {
  it('create an instance', () => {
    const pipe = new YtViewCountPipe();
    expect(pipe).toBeTruthy();
  });
});
